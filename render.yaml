services:
  - type: web
    name: techecommerce
    repo: https://github.com/Bizarbon/Project.git
    branch: techecommerce-deploy
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm --prefix backend ci --omit=dev
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: 22
      - key: JWT_SECRET
        generateValue: true
      - key: MONGO_URI
        sync: false
      - key: PAYMENT_GATEWAY_MODE
        value: mock
